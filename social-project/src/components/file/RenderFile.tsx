
type Props = {
  documents: any[]
  url?: string
  setUploadFile: (e: any) => void
  className?: string
  disabled?: boolean
  step?: string
}

const RenderFile = ({documents, setUploadFile, url = '', className, disabled, step}: Props) => {
  return <div></div>
}

export default RenderFile
